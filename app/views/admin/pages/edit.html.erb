<div class="card">
  <div class="section-header">
    <h1>Edit "<%= @page.slug.capitalize %>" page</h1>
    <p class="section-subtitle">
      Update the content shown on the public <%= @page.slug %> page.
    </p>
  </div>

  <%= render "form" %>
</div>
